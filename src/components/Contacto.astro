<section class="mt-16 bg-slate-900/40 p-8 rounded-lg shadow-lg w-full flex flex-col justify-center" id="contact">
    <h2 class="text-4xl font-bold mb-6 text-white text-center">Contáctame</h2>
    <form class="space-y-4" id="form-contact">
        <div>
            <input type="text" placeholder="Nombre" required class="w-full p-3 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500">
        </div>
        <div>
            <input type="email" placeholder="Correo" required class="w-full p-3 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500">
        </div>
        <div>
            <input type="text" placeholder="Asunto" required class="w-full p-3 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500">
        </div>
        <div>
            <textarea placeholder="Mensaje" required class="w-full p-3 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500 h-32"></textarea>
        </div>
        <div>
            <input type="submit" value="Enviar" class="w-full p-3 bg-blue-500 text-white rounded hover:bg-blue-600 cursor-pointer transition">
        </div>
    </form>
</section>

<script>
    document.getElementById('form-contact').addEventListener('submit', function(e) {
        e.preventDefault();
        alert('Mensaje enviado');
    });
</script>